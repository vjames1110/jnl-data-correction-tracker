[pytest]
python_files = tests.py test_*.py *_tests.py
addopts = -ra --strict-markers
testpaths = tests apps
markers =
    django_db: mark tests that require database access.
    real_scope: run with the real Project Monitor task-grant rules (by default every progress task is granted on every site to Project Incharge and Project Manager so feature tests stay focused).
